name = "shogun-hobby-shop"
compatibility_date = "2026-08-16"

[pages]
pages_build_output_dir = "frontend"

# D1 is bound to the Pages project in Cloudflare Dashboard.
# Binding variable name must be: DB
# Do not create a second database.
